services:
  redis:
    image: "redis:alpine"
    networks:
      - medihelp-network

  server:
    build:
      context: .
      dockerfile: server/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - REDIS_HOST=redis
      - PYTHONPATH=/app
    depends_on:
      - redis
      - ocr_worker
    networks:
      - medihelp-network

  ocr_worker:
    image: ocr_worker_image
    build:
      context: ./ocr_worker
      dockerfile: Dockerfile
    environment:
      - REDIS_HOST=redis
      - PYTHONPATH=/app
    depends_on:
      - redis
    networks:
      - medihelp-network

networks:
  medihelp-network:

